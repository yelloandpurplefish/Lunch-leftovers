# 食安守護者 - 專案架構書

## 📋 專案概述

**專案名稱：** 食安守護者 Food Guardian  
**專案類型：** 校園食安教育遊戲化應用  
**技術棧：** HTML5 + JavaScript + Firebase Firestore + Firebase Authentication  
**部署目標：** Web應用，支援登入系統和雲端資料同步

---

## 🏗️ 現有系統分析

### 目前功能模組

#### 1. 使用者管理
- **現狀：** 使用者輸入名字，儲存在 localStorage
- **問題：** 無身分驗證，資料不安全，無法跨裝置同步

#### 2. 虛擬貨幣系統
- **E幣：** 主要貨幣，用於兌換和抽獎
- **S幣：** 次要貨幣，用於特定兌換
- **積分：** 排行榜依據
- **現狀：** 儲存在 localStorage，易於作弊

#### 3. 任務系統
- **光盤行動：** 完成午餐光盤行動，獲得 E幣+10、S幣+5、積分+30
- **剩食獎勵：** 根據剩食重量計算獎勵，需先填寫剩食分析
- **冷卻機制：** 24小時冷卻時間
- **現狀：** 時間戳記儲存在 localStorage

#### 4. 問卷系統
- **功能：** 每日填寫最喜歡/最不喜歡的菜色
- **冷卻機制：** 24小時冷卻
- **現狀：** 資料僅顯示，未儲存

#### 5. 抽獎系統
- **消耗：** 10 E幣
- **獎勵等級：** 大獎(5%)、普通(30%)、小獎(65%)
- **現狀：** 隨機邏輯在前端，可被操控

#### 6. 兌換商城
- **物品：** 環保杯(50E)、環保餐具(80E)、文具禮包(30S)
- **現狀：** 扣款邏輯在前端

#### 7. 排行榜
- **現狀：** 靜態數據，非真實排行

#### 8. 剩食分析
- **功能：** 分析各項菜色剩食量，找出最多/最少
- **現狀：** 前端計算，資料未儲存

---

## 🔄 後端遷移策略

### 需要移至後端的資料與驗證

#### 🔴 必須移至後端（安全關鍵）

1. **使用者資料**
   - 使用者ID、姓名、Email
   - E幣、S幣、積分餘額
   - 建立時間、最後登入時間

2. **貨幣交易記錄**
   - 所有貨幣增減操作
   - 交易時間、原因、金額
   - 防止作弊和異常檢測

3. **任務完成記錄**
   - 光盤行動完成時間
   - 剩食獎勵領取記錄
   - 問卷提交記錄
   - 24小時冷卻驗證

4. **抽獎記錄**
   - 抽獎時間、消耗、結果
   - 隨機邏輯在後端執行
   - 獎勵發放記錄

5. **兌換記錄**
   - 兌換物品、消耗幣別、時間
   - 庫存管理（如需要）

#### 🟡 建議移至後端（資料完整性）

1. **剩食分析資料**
   - 每日各項菜色剩食量
   - 用於統計分析和報表

2. **問卷資料**
   - 每日喜好調查結果
   - 用於菜色改進參考

3. **排行榜資料**
   - 真實的積分排行
   - 定期更新

#### 🟢 可保留在前端

1. **UI狀態**
   - 當前頁面顯示
   - 滾動位置
   - 彈窗狀態

2. **暫存資料**
   - 表單輸入中
   - 未提交的分析結果

---

## 📊 Firestore 資料表設計

### 1. users (使用者表)
```javascript
{
  userId: string,           // Firebase Auth UID
  email: string,            // Email (選填)
  displayName: string,      // 顯示名稱
  eCoin: number,            // E幣餘額
  sCoin: number,            // S幣餘額
  score: number,            // 積分
  createdAt: timestamp,    // 建立時間
  lastLoginAt: timestamp,   // 最後登入時間
  schoolId: string,         // 學校ID (選填，用於分校排行)
  classId: string,          // 班級ID (選填)
  role: string,             // 角色: 'student' | 'teacher' | 'admin'
  isActive: boolean,        // 帳號是否啟用
}
```

### 2. task_records (任務記錄表)
```javascript
{
  recordId: string,          // 記錄ID
  userId: string,           // 使用者ID
  taskType: string,         // 任務類型: 'light_disc' | 'leftover_reward' | 'survey'
  completedAt: timestamp,   // 完成時間
  rewards: {                // 獲得獎勵
    eCoin: number,
    sCoin: number,
    score: number
  },
  metadata: {               // 額外資料
    leftoverWeight: number,      // 剩食重量 (剩食獎勵)
    foodAnalysis: object,       // 剩食分析資料
    favoriteFood: string,       // 最喜歡菜色 (問卷)
    hateFood: string           // 最不喜歡菜色 (問卷)
  }
}
```

### 3. lottery_records (抽獎記錄表)
```javascript
{
  recordId: string,          // 記錄ID
  userId: string,           // 使用者ID
  cost: number,             // 消耗E幣
  prizeType: string,        // 獎勵類型: 'grand' | 'normal' | 'small'
  prizeName: string,        // 獎勵名稱
  prizeValue: number,       // 獎勵價值
  drawnAt: timestamp,       // 抽獎時間
  isGrandPrize: boolean     // 是否為大獎
}
```

### 4. exchange_records (兌換記錄表)
```javascript
{
  recordId: string,          // 記錄ID
  userId: string,           // 使用者ID
  itemName: string,         // 物品名稱
  itemType: string,         // 幣別類型: 'E' | 'S'
  cost: number,             // 消耗金額
  exchangedAt: timestamp,   // 兌換時間
  status: string            // 狀態: 'pending' | 'completed' | 'cancelled'
}
```

### 5. daily_menu (每日菜單表)
```javascript
{
  menuId: string,            // 菜單ID
  date: date,               // 日期 (YYYY-MM-DD)
  items: [                  // 菜色項目
    {
      name: string,         // 菜色名稱
      category: string      // 分類: 'main' | 'side' | 'soup' | 'fruit'
    }
  ],
  isActive: boolean         // 是否啟用
}
```

### 6. leftover_analysis (剩食分析表)
```javascript
{
  analysisId: string,       // 分析ID
  userId: string,          // 使用者ID
  date: date,              // 日期 (YYYY-MM-DD)
  foodItems: [             // 各項菜色剩食量
    {
      foodName: string,    // 菜色名稱
      leftoverGrams: number // 剩食克數
    }
  ],
  totalLeftover: number,    // 總剩食量
  mostLeftover: string,    // 剩最多菜色
  leastLeftover: string,    // 剩最少菜色
  analyzedAt: timestamp     // 分析時間
}
```

### 7. reward_items (獎勵物品表)
```javascript
{
  itemId: string,           // 物品ID
  name: string,             // 物品名稱
  description: string,      // 描述
  costType: string,         // 幣別: 'E' | 'S'
  cost: number,             // 成本
  category: string,         // 分類: 'exchange' | 'lottery'
  stock: number,            // 庫存 (null表示無限)
  isActive: boolean,        // 是否啟用
  imageUrl: string          // 圖片URL (選填)
}
```

### 8. system_config (系統配置表)
```javascript
{
  configId: string,         // 配置ID
  key: string,             // 配置鍵
  value: any,               // 配置值
  description: string,      // 描述
  updatedAt: timestamp      // 更新時間
}
```

**配置範例：**
- `lottery_cost`: 10 (抽獎成本)
- `task_cooldown_hours`: 24 (任務冷卻小時)
- `reward_multipliers`: {剩食獎勵倍率配置}

---

## 🔌 API 接口設計

### 認證相關

#### POST /api/auth/register
**描述：** 註冊新使用者  
**請求：**
```json
{
  "email": "user@example.com",
  "password": "password123",
  "displayName": "王小明",
  "schoolId": "school001",
  "classId": "class001"
}
```
**回應：**
```json
{
  "success": true,
  "userId": "uid123",
  "token": "jwt_token_here"
}
```

#### POST /api/auth/login
**描述：** 使用者登入  
**請求：**
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```
**回應：**
```json
{
  "success": true,
  "userId": "uid123",
  "token": "jwt_token_here",
  "userData": {
    "displayName": "王小明",
    "eCoin": 100,
    "sCoin": 50,
    "score": 500
  }
}
```

#### POST /api/auth/logout
**描述：** 使用者登出  
**請求：**
```json
{
  "token": "jwt_token_here"
}
```
**回應：**
```json
{
  "success": true
}
```

### 使用者資料相關

#### GET /api/user/profile
**描述：** 獲取使用者資料  
**請求：**
```json
{
  "token": "jwt_token_here"
}
```
**回應：**
```json
{
  "success": true,
  "userData": {
    "userId": "uid123",
    "displayName": "王小明",
    "eCoin": 100,
    "sCoin": 50,
    "score": 500,
    "createdAt": "2024-01-01T00:00:00Z",
    "lastLoginAt": "2024-01-15T10:30:00Z"
  }
}
```

#### PUT /api/user/profile
**描述：** 更新使用者資料  
**請求：**
```json
{
  "token": "jwt_token_here",
  "displayName": "李大明"
}
```
**回應：**
```json
{
  "success": true
}
```

### 任務相關

#### POST /api/task/complete-light-disc
**描述：** 完成光盤行動任務  
**請求：**
```json
{
  "token": "jwt_token_here"
}
```
**回應：**
```json
{
  "success": true,
  "message": "任務完成！",
  "rewards": {
    "eCoin": 10,
    "sCoin": 5,
    "score": 30
  },
  "nextAvailableAt": "2024-01-16T10:30:00Z"
}
```

#### POST /api/task/claim-leftover-reward
**描述：** 領取剩食獎勵  
**請求：**
```json
{
  "token": "jwt_token_here",
  "leftoverWeight": 25,
  "foodAnalysis": [
    {"foodName": "香酥雞腿", "leftoverGrams": 10},
    {"foodName": "高麗菜", "leftoverGrams": 5},
    {"foodName": "蒸蛋", "leftoverGrams": 8},
    {"foodName": "蘋果", "leftoverGrams": 2},
    {"foodName": "玉米濃湯", "leftoverGrams": 0}
  ]
}
```
**回應：**
```json
{
  "success": true,
  "message": "獎勵已發放！",
  "rewards": {
    "eCoin": 15,
    "sCoin": 8
  },
  "nextAvailableAt": "2024-01-16T10:30:00Z"
}
```

#### POST /api/task/submit-survey
**描述：** 提交問卷  
**請求：**
```json
{
  "token": "jwt_token_here",
  "favoriteFood": "香酥雞腿",
  "hateFood": "高麗菜"
}
```
**回應：**
```json
{
  "success": true,
  "message": "問卷已送出！",
  "nextAvailableAt": "2024-01-16T10:30:00Z"
}
```

### 抽獎相關

#### POST /api/lottery/spin
**描述：** 進行抽獎  
**請求：**
```json
{
  "token": "jwt_token_here"
}
```
**回應：**
```json
{
  "success": true,
  "prize": {
    "type": "grand",
    "name": "豪華文具組",
    "value": 60,
    "isGrandPrize": true
  },
  "remainingECoin": 90
}
```

#### GET /api/lottery/history
**描述：** 獲取抽獎歷史  
**請求：**
```json
{
  "token": "jwt_token_here",
  "limit": 10
}
```
**回應：**
```json
{
  "success": true,
  "history": [
    {
      "prizeName": "豪華文具組",
      "prizeType": "grand",
      "prizeValue": 60,
      "drawnAt": "2024-01-15T10:30:00Z"
    }
  ]
}
```

### 兌換相關

#### POST /api/exchange/redeem
**描述：** 兌換物品  
**請求：**
```json
{
  "token": "jwt_token_here",
  "itemId": "item001"
}
```
**回應：**
```json
{
  "success": true,
  "message": "兌換成功！",
  "remainingCoin": 50,
  "exchangeRecord": {
    "recordId": "rec001",
    "itemName": "環保杯",
    "cost": 50
  }
}
```

#### GET /api/exchange/items
**描述：** 獲取可兌換物品列表  
**請求：**
```json
{
  "token": "jwt_token_here"
}
```
**回應：**
```json
{
  "success": true,
  "items": [
    {
      "itemId": "item001",
      "name": "環保杯",
      "costType": "E",
      "cost": 50,
      "stock": 100
    }
  ]
}
```

#### GET /api/exchange/history
**描述：** 獲取兌換歷史  
**請求：**
```json
{
  "token": "jwt_token_here",
  "limit": 10
}
```
**回應：**
```json
{
  "success": true,
  "history": [
    {
      "itemName": "環保杯",
      "cost": 50,
      "exchangedAt": "2024-01-15T10:30:00Z",
      "status": "completed"
    }
  ]
}
```

### 排行榜相關

#### GET /api/leaderboard
**描述：** 獲取排行榜  
**請求：**
```json
{
  "token": "jwt_token_here",
  "limit": 10,
  "schoolId": "school001"  // 選填，指定學校
}
```
**回應：**
```json
{
  "success": true,
  "leaderboard": [
    {
      "rank": 1,
      "displayName": "王小明",
      "score": 3500,
      "schoolId": "school001"
    },
    {
      "rank": 2,
      "displayName": "陳小華",
      "score": 3300,
      "schoolId": "school001"
    }
  ],
  "myRank": {
    "rank": 15,
    "score": 500
  }
}
```

### 剩食分析相關

#### POST /api/analysis/submit
**描述：** 提交剩食分析  
**請求：**
```json
{
  "token": "jwt_token_here",
  "foodItems": [
    {"foodName": "香酥雞腿", "leftoverGrams": 10},
    {"foodName": "高麗菜", "leftoverGrams": 5}
  ]
}
```
**回應：**
```json
{
  "success": true,
  "analysisId": "analysis001",
  "mostLeftover": "香酥雞腿",
  "leastLeftover": "高麗菜",
  "totalLeftover": 15
}
```

#### GET /api/analysis/stats
**描述：** 獲取剩食統計 (管理員用)  
**請求：**
```json
{
  "token": "jwt_token_here",
  "startDate": "2024-01-01",
  "endDate": "2024-01-31",
  "schoolId": "school001"
}
```
**回應：**
```json
{
  "success": true,
  "stats": {
    "totalLeftover": 5000,
    "averagePerStudent": 25,
    "mostWastedFood": "香酥雞腿",
    "leastWastedFood": "玉米濃湯"
  }
}
```

### 系統配置相關

#### GET /api/config
**描述：** 獲取系統配置  
**請求：**
```json
{
  "token": "jwt_token_here"
}
```
**回應：**
```json
{
  "success": true,
  "config": {
    "lotteryCost": 10,
    "taskCooldownHours": 24,
    "rewardMultipliers": {
      "low": {"eCoin": 20, "sCoin": 10},
      "medium": {"eCoin": 15, "sCoin": 8},
      "high": {"eCoin": 10, "sCoin": 5}
    }
  }
}
```

---

## 🔐 安全性考量

### 1. 認證與授權
- 使用 Firebase Authentication 進行使用者認證
- JWT Token 驗證所有 API 請求
- 角色權限控制 (student/teacher/admin)

### 2. 資料驗證
- 所有輸入資料在前端和後端雙重驗證
- 防止 SQL 注入 (使用 Firestore 自動化防護)
- 防止 XSS 攻擊

### 3. 速率限制
- API 請求頻率限制
- 防止惡意刷獎
- 任務冷卻時間強制執行

### 4. 資料加密
- 敏感資料加密儲存
- HTTPS 通訊加密
- Token 有效期管理

### 5. 作弊防護
- 關鍵邏輯在後端執行
- 異常行為檢測
- 交易記錄審計

---

## 🚀 部署架構

### 前端部署
- **平台：** Firebase Hosting / Vercel / Netlify
- **CDN：** 自動全球分發
- **HTTPS：** 自動 SSL 憑證

### 後端部署
- **資料庫：** Firebase Firestore
- **認證：** Firebase Authentication
- **API：** Firebase Cloud Functions (可選)
- **儲存：** Firebase Storage (圖片/檔案)

### 架構圖
```
┌─────────────┐
│   用戶端     │
│  (Browser)  │
└──────┬──────┘
       │ HTTPS
       ↓
┌─────────────┐
│ Firebase    │
│ Hosting     │
└──────┬──────┘
       │
       ├──────────────┐
       ↓              ↓
┌─────────────┐  ┌─────────────┐
│ Firebase    │  │ Firebase    │
│ Auth        │  │ Firestore   │
└─────────────┘  └─────────────┘
       │              │
       └──────┬───────┘
              ↓
     ┌─────────────┐
     │ Cloud       │
     │ Functions   │
     │ (可選)      │
     └─────────────┘
```

---

## 📅 開發階段規劃

### Phase 1: 基礎架構 (Week 1-2)
- [ ] Firebase 專案設定
- [ ] 登入/註冊頁面開發
- [ ] 使用者資料表設計與實作
- [ ] 基礎 API 開發

### Phase 2: 核心功能遷移 (Week 3-4)
- [ ] 貨幣系統後端化
- [ ] 任務系統後端化
- [ ] 冷卻機制後端驗證
- [ ] 前端整合測試

### Phase 3: 進階功能 (Week 5-6)
- [ ] 抽獎系統後端化
- [ ] 兌換系統後端化
- [ ] 排行榜實作
- [ ] 剩食分析資料儲存

### Phase 4: 優化與部署 (Week 7-8)
- [ ] 效能優化
- [ ] 安全性檢測
- [ ] 部署上線
- [ ] 監控與維護

---

## 📝 備註

### 技術選擇理由
1. **Firebase Firestore**: NoSQL 資料庫，適合遊戲化應用，即時同步，自動擴展
2. **Firebase Authentication**: 完整的認證解決方案，支援多種登入方式
3. **Cloud Functions**: 無需管理伺服器，按需付費

### 未來擴展性
- 可新增多校園支援
- 可新增教師管理後台
- 可新增家長監控功能
- 可新增數據分析儀表板

### 維護考量
- 定期備份重要資料
- 監控 API 使用量
- 定期更新安全性設定
- 收集使用者反饋改進

---

**文件版本：** 1.0  
**最後更新：** 2024-01-15  
**維護者：** 開發團隊

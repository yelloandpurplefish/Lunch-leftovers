# 伺服器配置
PORT=3000
NODE_ENV=development

# Firebase Admin SDK 配置
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com

# 注意：前端直接使用 Firebase Config，請在前端 firebase-config.js 中配置
# 後端現在主要用於管理功能，可選擇是否啟用
{
  "name": "food-guardian",
  "version": "1.0.0",
  "description": "食安守護者 - 校園食安教育遊戲化應用",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd backend && npm run dev",
    "postinstall": "cd backend && npm install",
    "seed": "node scripts/seed-test-users.js",
    "test:firebase": "node -e \"require('dotenv').config({path:'backend/.env'}); const {db} = require('./backend/config/firebase'); db.collection('users').limit(1).get().then(()=>console.log('Firestore OK')).catch(e=>console.error('Firestore Failed', e))\""
  },
  "keywords": ["food", "safety", "education", "firebase", "dynamic-web"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "firebase-admin": "^11.11.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}